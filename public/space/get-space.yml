info:
  name: get-space
  type: graphql
  seq: 1

graphql:
  method: POST
  url: "{{graphql_url}}"
  body:
    query: |-
      query {
        space(id: "7e1af088-a693-47b9-a1cd-c7a81adfcf00") {
          id
          name
          slug
          sport
          sportType
          floorType
          maxCapacity
          lighting
          roofing
          changingRoom
          parking
          stands
          airConditioner
          wifi
          enabled
          transformable
          depositType
          cancellationPolicy
          allowedDurations
          createdAt
          updatedAt
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
