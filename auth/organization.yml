info:
  name: organization
  type: http
  seq: 2

http:
  method: POST
  url: "{{api_url}}/auth/organization/set-active"
  body:
    type: json
    data: |-
      {
        "organizationId": "70dfa2a6-af79-4b55-9816-4171e1f49ece"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
