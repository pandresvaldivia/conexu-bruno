info:
  name: organization
  type: http
  seq: 2

http:
  method: POST
  url: "{{api_url}}/auth/organization/set-active"
  body:
    type: json
    data: |-
      {
        "organizationId": "f168d1f5-4fc4-4bf1-9f28-76afc8a0ae4e"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
