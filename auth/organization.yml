info:
  name: organization
  type: http
  seq: 2

http:
  method: POST
  url: "{{api_url}}/auth/organization/set-active"
  body:
    type: json
    data: |-
      {
        "organizationId": "f011eeef-f486-4888-a77a-06f463956691"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
