info:
  name: get-session
  type: http
  seq: 3

http:
  method: GET
  url: "{{api_url}}/auth/get-session"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
