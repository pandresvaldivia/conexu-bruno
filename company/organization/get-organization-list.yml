info:
  name: get-organization-list
  type: http
  seq: 3

http:
  method: GET
  url: "{{api_url}}/auth/organization/list"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
